# GitHub Repo Opener

A script that opens GitHub repositories in Chrome.

## Usage

**Terminal:** `gitopen`  
**Hotkey:** Set to run `bash /home/trauco/scripts/git/open-github-repo.sh`

## Features
- Opens current repo URL if in git directory
- Opens your profile if not in a git directory
- Works with both SSH and HTTPS URLs
